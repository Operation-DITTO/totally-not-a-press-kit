ctrlhook-blocking-input.mov - a demo video showing control being re-routed to an empty function (and subsequently completely disabled)

---

inject-right-stick.jpg - showing of dumping fake right stick values into sceCtrlData.rsrv[]